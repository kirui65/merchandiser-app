# Backend API base URL. localhost will NOT resolve from a physical device —
# use your machine's LAN IP when testing on hardware, or the deployed Render URL.
EXPO_PUBLIC_API_URL=http://localhost:4000/api
