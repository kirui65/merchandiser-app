import React, { useEffect, useMemo, useState } from 'react';
import client from '../api/client';
import MapView from '../components/MapView';
import Card from '../components/Card';
const today = new Date().toISOString().slice(0, 10);
export default function RouteReplayPage() { const [date,setDate]=useState(today); const [repId,setRepId]=useState(''); const [reps,setReps]=useState([]); const [route,setRoute]=useState(null); const [outlets,setOutlets]=useState([]); const [position,setPosition]=useState(0); const [error,setError]=useState(null); useEffect(()=>{client.get('/reps').then(({data})=>{setReps(data.reps);if(data.reps[0])setRepId(data.reps[0].id)}).catch(()=>setError('Failed to load reps'))},[]); useEffect(()=>{if(!repId)return;setError(null);Promise.all([client.get('/routes',{params:{date,repId}}),client.get('/outlets',{params:{repId}})]).then(([routeResponse,outletResponse])=>{setRoute(routeResponse.data.route);setOutlets(outletResponse.data.outlets.filter((outlet)=>routeResponse.data.route.plannedOutletIds.includes(outlet.id)));setPosition(routeResponse.data.route.pings.length?routeResponse.data.route.pings.length-1:0)}).catch(()=>setError('Failed to load route'))},[date,repId]); const visiblePings=useMemo(()=>route?.pings.slice(0,position+1)||[],[route,position]); const missedCount=route?route.plannedOutletIds.filter((id)=>!route.visitedOutletIds.includes(id)).length:0; return <><div className="page-heading"><span className="eyebrow">FIELD INTELLIGENCE</span><h1>Route replay</h1><p className="muted">Review movement and outlet coverage by representative.</p></div><Card><div className="grid-2"><div className="field"><label>Representative</label><select className="control" value={repId} onChange={(event)=>setRepId(event.target.value)}><option value="">Select rep</option>{reps.map((rep)=><option key={rep.id} value={rep.id}>{rep.name}</option>)}</select></div><div className="field"><label>Date</label><input className="control" type="date" value={date} onChange={(event)=>setDate(event.target.value)} /></div></div></Card>{error&&<div className="error-banner">{error}</div>}{route&&<Card title={`${route.pings.length} pings · ${route.visitedOutletIds.length} visited · ${missedCount} missed`}><MapView pings={visiblePings} outlets={outlets} visitedOutletIds={route.visitedOutletIds}/><div className="field"><label>Replay position</label><input type="range" min="0" max={Math.max(route.pings.length-1,0)} value={position} onChange={(event)=>setPosition(Number(event.target.value))}/></div>{visiblePings.at(-1)&&<small className="muted">Latest visible ping: {new Date(visiblePings.at(-1).timestamp).toLocaleString()}</small>}</Card>}</>; }
